import Hashtag from "../../../components/Hashtag";
import LoginNav from "../../../components/LoginNav";

const page = () => {
  return (
    <>
      <LoginNav />
      <Hashtag />
    </>
  );
};

export default page;
